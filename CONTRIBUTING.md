# 貢献ガイド

変更提案や不具合報告の前に [開発方針](docs/development-policy.md) と [ロードマップ](docs/roadmap.md) を確認してください。現在は③のXLSX解析をSwiftで実装し、架空データの比較テストを追加しています。iOSビルド・実機の解析結果確認は確認待ちです。

## 不具合報告

再現手順、期待した結果、実際の結果、アプリの version / build、iOS のバージョンを記載してください。配布の問題では AltStore Classic / AltServer のバージョンとエラー内容も役立ちます。

学校の実資料、氏名、メールアドレス、OneDrive 共有リンク、認証情報、個人情報の入ったログや画像は Issue や Pull Request に添付しないでください。解析問題は実資料を公開せず、架空の最小入力と期待する結果で説明してください。

## Pull Request

1. [開発環境](docs/development.md) に従って clone と Git の公開用作者情報を設定します。
2. 変更用ブランチを作成し、目的に必要な変更を加えます。
3. 変更に合った検証と配布スクリプトのテストを行います。
4. 関連する README・手順・仕様を更新します。
5. コミット対象に実資料・個人情報・認証情報・一時ファイルが含まれないことを確認します。
6. Pull Request に変更の理由、動作の違い、実施した検証、未確認の範囲を記載します。

公開テストの入力・期待結果には実際の教師名・教科名を使用せず、「架空教員A」「架空科目A」など、完全に架空のデータを使用します。実資料の名前だけを変えたデータは使用しません。既存コードや外部素材を取り込む場合は、出典とライセンスを確認してください。

## 実装時の基準

- 曖昧な学校資料を推測で時間割へ反映しない。
- 取得・解析失敗で前回正常データを破壊しない。
- 不要な一時ファイルを溜めず、自分の処理が作成したものだけを片付ける。
- 未確認の CI・実機動作を確認済みと記載しない。
- 依存関係や権限は、その機能に必要になった時点で導入する。

プロジェクトのライセンスは現在未選定です。
