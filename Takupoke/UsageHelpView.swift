import SwiftUI

struct UsageHelpView: View {
    var body: some View {
        List {
            Section("時間割を表示する") {
                Text("「データ取得」で学校アカウント認証を行い、一覧と名称対応表を取得します。")
                Text("「ファイル選択」で通常時間割PDFと時間割変更XLSXを選び、時間割のクラスを設定します。試験時間割・試験返却時間割のPDFも選べます。")
                Text("ファイル選択後に自動で解析します。再解析は各ファイルの詳細にある「解析する」から行えます。")
            }
            Section("時間割変更の年度") {
                Text("年のない日付には学校年度を補います。空欄なら現在の学校年度を使い、1〜3月はその翌年として扱います。補完年度は時間割変更の詳細から変更できます。")
            }
            Section("OneDriveの準備") {
                Text("対象ファイルの「⋯」から「オフラインで使用可能にする」を選び、ダウンロードの完了を待ってください。")
                Text("iPhoneの「設定」→「一般」→「Appのバックグラウンド更新」でOneDriveを有効にしてください。低電力モードではバックグラウンド更新が停止します。")
            }
            Section("更新が反映されないとき") {
                Text("OneDriveを開いて同期状況を確認し、「ファイル」で対象を開いてから、たくポケへ戻ってください。読み取れない場合はファイルを選び直してください。")
                Text("選択済みファイルは起動・復帰時と表示中の変更通知で確認し、内容が変わったときに再解析します。最新版が届く時期はOneDriveの同期状況により異なります。")
            }
            Section("保存期間") {
                Text("4月1日・10月1日の切替後、保存済みの学校ファイル・解析結果・一覧・名称対応表を削除します。ファイルを選び直し、学校アカウントで再取得してください。")
            }
        }
        .navigationTitle("使い方")
    }
}
